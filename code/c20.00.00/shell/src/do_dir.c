/**
 * 打印文本文件内容命令
 *
 * 创建时间：2021年8月5日
 * 作者：李述铜
 * 联系邮箱: 527676163@qq.com
 */
#include <stdio.h>
#include <errno.h>
#include <stddef.h>
#include <unistd.h>
#include "do_dir.h"
#include "cli.h"

/**
 * 打印文本文件内容
 */
int do_dir (int argc, char ** argv) {
//	chdir("a");
//	DIR * dp;
//	struct dirent * entry;
//	if (argc != 2) {
//		printf("usage: ls directory_name");
//	}
//
//	// 打开目录文件
//	if ( (dp = opendir(argv[1])) == NULL) {
//		fprintf(stderr, "can't open directory: %s", argv[1]);
//	}
//
//	while ((entry = readdir(dp)) != NULL) {
//		printf("name:%s-type:%d-position:%ld\n", dirp->d_name, dirp->d_type, telldir(dp));
//	}
//
//	closedir(dp);
	return 0;
}
